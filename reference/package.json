{
  "name": "learning-sessions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel ./**/*.html",
    "build": "parcel build ./**/*.html --public-url /presentations",
    "clean": "rm -rf dist .cache",
    "refresh": "npm run clean && npm start",
    "publish":"gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "highlight.js": "^10.0.0",
    "tailwindcss": "^1.3.3"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4"
  }
}
